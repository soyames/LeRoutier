# USSD Service

Provider adapter/session layer for essential LeRoutier workflows on USSD-capable networks.

It must call the same authoritative backend/domain rules as the web applications. Provider-specific session formats belong in adapters, not in the core booking model.
