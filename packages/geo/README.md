# Geo Package

Shared geospatial abstractions for coordinates, stops, bounding areas, geocoding/routing contracts and map-provider adapters.

OpenStreetMap-compatible data/rendering is the default direction; provider-specific APIs remain replaceable.
